# protractor-http-mock-test
Small AngularJS test application for testing the protractor-http-mock plugin.

# Install
Use `npm install`

# Run
Start the HTTP server with `npm start`

# Test
Run protractor tests with `npm test`
